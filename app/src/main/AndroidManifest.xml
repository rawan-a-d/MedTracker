<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medtracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/medtrack_logo_no_text"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedTracker">
        <activity android:name=".MyMedicinesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".home.MainActivity"></activity>

        <activity android:name=".choose_profile.ChooseProfileActivity"></activity>

        <activity android:name=".LoginActivity">

        </activity>
    </application>
</manifest>